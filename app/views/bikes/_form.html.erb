<%= form_for(@bike) do |f| %>
  <% if @bike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bike.errors.count, "error") %> prevented this bike from being saved:</h2>
      <ul>
      <% @bike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- PUT FORM ITEMS HERE -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
