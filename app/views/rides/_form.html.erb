<%= form_for(@ride) do |f| %>
  <% if @ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride.errors.count, "error") %> prevented this ride from being saved:</h2>
      <ul>
      <% @ride.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- PUT FORM ITEMS HERE -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
