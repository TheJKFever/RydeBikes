<%= form_for(@network) do |f| %>
  <% if @network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@network.errors.count, "error") %> prevented this network from being saved:</h2>
      <ul>
      <% @network.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- PUT FORM ITEMS HERE -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
